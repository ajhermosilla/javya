# Javya development environment
# Auto-loads when entering the directory (via direnv)

# Load .env file (contains DB credentials, API keys, etc.)
dotenv_if_exists .env

# Add local bin to PATH (for project scripts)
PATH_add backend

# Optional: Uncomment to use local Python venv instead of Docker
# layout_python_venv
